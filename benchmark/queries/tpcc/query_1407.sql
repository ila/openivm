-- {"operators": "INNER_JOIN,AGGREGATE,HAVING", "complexity": "high", "is_incremental": true, "has_nulls": false, "has_cast": false, "has_case": false, "tables": "CUSTOMER,ITEM,OORDER,ORDER_LINE"}
SELECT c.C_W_ID, c.C_ID, COUNT(*) AS cnt FROM CUSTOMER c JOIN OORDER o ON c.C_ID = o.O_C_ID AND c.C_W_ID = o.O_W_ID JOIN ORDER_LINE ol ON o.O_ID = ol.OL_O_ID AND o.O_W_ID = ol.OL_W_ID JOIN ITEM i ON ol.OL_I_ID = i.I_ID GROUP BY c.C_W_ID, c.C_ID HAVING COUNT(*) > 1;
