-- {"operators": "INNER_JOIN,AGGREGATE,DISTINCT", "complexity": "high", "is_incremental": true, "has_nulls": false, "has_cast": true, "has_case": false, "tables": "WAREHOUSE,DISTRICT,CUSTOMER,OORDER"}
SELECT w.W_ID, d.D_ID, COUNT(DISTINCT CAST((w.W_ID) AS VARCHAR)) AS uniq, COUNT(*) AS cnt FROM WAREHOUSE w JOIN DISTRICT d ON w.W_ID = d.D_W_ID JOIN CUSTOMER c ON d.D_W_ID = c.C_W_ID AND d.D_ID = c.C_D_ID JOIN OORDER o ON c.C_ID = o.O_C_ID AND c.C_W_ID = o.O_W_ID GROUP BY w.W_ID, d.D_ID;
