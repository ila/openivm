-- {"operators": "INNER_JOIN,AGGREGATE,WINDOW,CTE,SUBQUERY", "complexity": "high", "is_incremental": true, "has_nulls": false, "has_cast": false, "has_case": false, "tables": "WAREHOUSE,DISTRICT,CUSTOMER,OORDER,ORDER_LINE", "delta": true}
WITH order_value AS (SELECT OL_W_ID, OL_D_ID, OL_O_ID, SUM(OL_AMOUNT) AS amt FROM d_ORDER_LINE GROUP BY OL_W_ID, OL_D_ID, OL_O_ID) SELECT w.W_ID, d.D_ID, c.C_ID, c.C_LAST, ov.amt, (ov.amt * (1 - c.C_DISCOUNT)) AS final_amt, SUM(ov.amt) OVER (PARTITION BY w.W_ID) AS w_total FROM d_WAREHOUSE w JOIN d_DISTRICT d ON w.W_ID = d.D_W_ID JOIN d_CUSTOMER c ON d.D_W_ID = c.C_W_ID AND d.D_ID = c.C_D_ID JOIN d_OORDER o ON c.C_W_ID = o.O_W_ID AND c.C_D_ID = o.O_D_ID AND c.C_ID = o.O_C_ID JOIN order_value ov ON o.O_W_ID = ov.OL_W_ID AND o.O_D_ID = ov.OL_D_ID AND o.O_ID = ov.OL_O_ID;
